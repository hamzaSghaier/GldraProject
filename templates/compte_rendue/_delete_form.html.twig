<form method="post" action="{{ path('compte_rendue_delete', {'id': compte_rendue.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compte_rendue.id) }}">
    <button class="btn">Delete</button>
</form>
